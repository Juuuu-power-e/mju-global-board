apiVersion: apps/v1
kind: Deployment
metadata:
  name: mju-global-board-be
  namespace: mju-board
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mju-global-board-be
  template:
    metadata:
      labels:
        app: mju-global-board-be
    spec:
      containers:
        - name: mju-global-board-be
          image: 3tmdwn3/mju-global-board-be:9ddc0f6550a4e4c9d8019be1e190dcd5e5d706e9
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            # 필요하면 프로파일, DB 설정 등 여기 추가
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"

---
apiVersion: v1
kind: Service
metadata:
  name: mju-global-board-be
  namespace: mju-board
spec:
  selector:
    app: mju-global-board-be
  ports:
    - port: 8080
      targetPort: 8080
